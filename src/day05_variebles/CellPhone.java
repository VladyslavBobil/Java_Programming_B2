package day05_variebles;

public class CellPhone {
    public static void main (String [ ] args) {
        String brand = "Apple",
                model = "14 pro Max",
                color = "black";
        double price = 1000.99;
        int storage = 128;
        boolean hasACamera = true;
        char sim = 'A';
        System.out.println(" I have an " + model + " from  " +  brand + " the color is " + color + " i paid in store " + price + "$ with the memory capacity " + storage + " GB " + " it have a good camara " + hasACamera + " have a type of sim card " + sim );
    }
}
